{
    "height": 24,
    // "modules-left": ["river/tags"],
    "modules-right": ["pulseaudio", "cpu", "memory", "clock"],
    "pulseaudio": {
        "format": "  {volume}%",
    },
    "cpu": {
        "format": "  {usage}%"
    },
    "memory": {
        "format": "  {percentage}%"
    },
    "clock": {
        "interval": 3,
        "format": "{:%a %b %d %I:%M:%S%p}"
    }
}
